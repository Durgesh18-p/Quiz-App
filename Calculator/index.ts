import {question} from "readline-sync"

const main = () => { 
  const firststring : string = question("What is your name : ")
  const operator : string = question("What is your name : ")
  const secondString : string = question("What is your name : ")
}


main()